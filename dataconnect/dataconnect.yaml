specVersion: "v1beta"
serviceId: "plantkeeper-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "plantkeeper-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
